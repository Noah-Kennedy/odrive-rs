language: rust

addons:
  apt:
    packages:
      - libudev-dev

rust:
  - stable
  - beta
  - nightly

matrix:
  allow_failures:
    - rust: nightly

arch:
  - amd64
  - arm64

jobs:
  include:
    - stage: Coverage
      addons:
        apt:
          packages:
            - libssl-dev
      arch: amd64
      rust: stable
      script:
        - cargo install cargo-tarpaulin -f
        - cargo tarpaulin --out Xml
        - bash <(curl -s https://codecov.io/bash)